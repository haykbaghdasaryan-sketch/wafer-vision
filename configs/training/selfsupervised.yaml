# Self-supervised SimCLR training configuration
training:
  mode: selfsupervised
  loss: ntxent
  epochs: 200
  batch_size: 256
  learning_rate: 3.0e-4
  weight_decay: 1.0e-6
  simclr_temperature: 0.5
  warmup_epochs: 10
  gradient_accumulation_steps: 4
  early_stopping_patience: 20

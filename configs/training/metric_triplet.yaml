# Metric learning with Triplet Loss configuration
training:
  mode: metric
  loss: triplet
  epochs: 100
  batch_size: 36  # P=9 * K=4
  learning_rate: 1.0e-4
  weight_decay: 1.0e-5
  margin: 0.2
  P_classes: 9
  K_samples: 4
  warmup_epochs: 5
  early_stopping_patience: 15

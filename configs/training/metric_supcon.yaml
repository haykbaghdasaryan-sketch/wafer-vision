# Metric learning with Supervised Contrastive Loss configuration
training:
  mode: metric
  loss: supcon
  epochs: 100
  batch_size: 36  # P=9 * K=4
  learning_rate: 1.0e-4
  weight_decay: 1.0e-5
  temperature: 0.07
  P_classes: 9
  K_samples: 4
  warmup_epochs: 5
  early_stopping_patience: 15
